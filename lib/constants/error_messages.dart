const LOCATION_SERVICES_DISABLED = 'Location services are disabled';

const LOCATION_SERVICES_PERMANENTLY_DISABLED =
    'Location permissions are permantly denied, enable in settings';

const LOCATION_PERMISSION_DENIED = 'Location permissions denied';

const NO_INTERNET_CONNECTION = 'No internet connection';

const CAN_NOT_REACH_SERVER = 'Failed to reach server';
