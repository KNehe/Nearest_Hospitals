const LOCATION_SERVICES_DISABLED = 'Location services are disabled';

const LOCATION_SERVICES_PERMANENTLY_DISABLED =
    'Location permission are permantly denied, enable in settings';

const LOCATION_PERMISSION_DENIED = 'Location permission denied';

const NO_INTERNET_CONNECTION = 'No internet connection';

const CAN_NOT_REACH_SERVER = 'Failed to reach server';

const LOCATION_PERMISSION_ONLY_WHILE_USE =
    'Location permission is always required';

const LOCATION_PERMISSION_AND_SERVICES_REQUIRED =
    'Location permission and services are required';

const UNKNOWN_ERROR = 'Unknown error occurred';
