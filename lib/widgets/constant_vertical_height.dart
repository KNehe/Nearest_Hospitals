import 'package:flutter/material.dart';

class ConstVerticalHeight extends StatelessWidget {
  const ConstVerticalHeight({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
    );
  }
}
